--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.110.3
uvicorn==0.30.1
pydantic==2.7.4

# CPU-only PyTorch to keep Railway image small and avoid CUDA pulls
torch==2.2.2+cpu

# Keep transformers + hub compatible with torch 2.2.x
transformers==4.40.2
huggingface_hub==0.23.2

# ABI-safe scientific stack (avoid NumPy 2.x issues)
numpy==1.26.4
pandas==2.2.2

# Required at import-time by current runtime modules
datasets==2.20.0

# FastAPI file upload support (/upload) and PDF parsing
python-multipart==0.0.9
pdfplumber==0.11.1

# Prevent protobuf-related runtime issues in transformer stack
protobuf==4.25.3

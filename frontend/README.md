# Frontend

React client for the Financial Pragmatic AI analyzer.

## Run

```bash
npm install
npm start
```

The app runs at http://localhost:3000 and calls the backend API at http://127.0.0.1:8000.
